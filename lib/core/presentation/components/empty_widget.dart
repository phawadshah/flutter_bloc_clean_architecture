import 'package:flutter/material.dart';

class KEmptyWidget extends StatelessWidget {
  const KEmptyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
